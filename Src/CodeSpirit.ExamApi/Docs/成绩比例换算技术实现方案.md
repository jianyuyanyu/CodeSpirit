# 成绩比例换算技术实现方案

## 1. 需求分析

### 业务需求
- 支持将非标准分值（如120分、150分等）的试卷成绩按特定比例自动转换为百分制标准分
- 满足国家职业标准对考试成绩「百分制」的强制要求
- 在试卷生成时预先设置换算规则，在成绩计算时自动应用

### 技术需求
- 扩展`GenerateRandomExamPaperDto`支持成绩换算配置
- 扩展数据模型支持成绩换算规则存储
- 实现成绩换算服务和逻辑
- 确保换算后的成绩仍然符合及格标准

## 2. 数据模型设计

### 2.1 新增成绩换算规则DTO

**删除文件：`Dtos/ExamPaper/ScoreConversionRule.cs`**
> 不再需要单独的换算规则DTO，直接在GenerateRandomExamPaperDto中添加换算相关字段
```

### 2.1 扩展试卷实体

**修改文件：`Data/Models/ExamPaper.cs`**
```csharp
// 在ExamPaper类中新增属性
/// <summary>
/// 是否启用成绩换算
/// </summary>
public bool EnableScoreConversion { get; set; } = false;

/// <summary>
/// 换算目标满分
/// </summary>
[Range(1, 1000)]
public int? ConversionTargetFullScore { get; set; }

/// <summary>
/// 换算目标及格分
/// </summary>
[Range(0, 1000)]
public int? ConversionTargetPassScore { get; set; }

/// <summary>
/// 换算小数保留位数
/// </summary>
[Range(0, 2)]
public int ConversionDecimalPlaces { get; set; } = 1;

/// <summary>
/// 换算比例（目标满分/原始满分，使用decimal确保精度）
/// </summary>
public decimal? ConversionRatio { get; set; }
```

### 2.2 扩展GenerateRandomExamPaperDto

**修改文件：`Dtos/ExamPaper/GenerateRandomExamPaperDto.cs`**
```csharp
// 在GenerateRandomExamPaperDto类中新增属性（在CategoryIds属性后添加）

/// <summary>
/// 是否启用成绩换算
/// </summary>
[DisplayName("启用成绩换算")]
[AmisSwitchField("启用成绩换算", false)]
public bool EnableScoreConversion { get; set; } = false;

/// <summary>
/// 换算目标满分（通常为100）
/// </summary>
[DisplayName("换算目标满分")]
[Required(ErrorMessage = "换算目标满分不能为空")]
[Range(1, 1000, ErrorMessage = "换算目标满分必须在1-1000之间")]
[AmisNumberField("换算目标满分", Min = 1, Max = 1000, DefaultValue = 100)]
public int ConversionTargetFullScore { get; set; } = 100;

/// <summary>
/// 换算目标及格分（通常为60）
/// </summary>
[DisplayName("换算目标及格分")]
[Range(0, 1000, ErrorMessage = "换算目标及格分必须在0-1000之间")]
[AmisNumberField("换算目标及格分", Min = 0, Max = 1000, DefaultValue = 60)]
public int ConversionTargetPassScore { get; set; } = 60;

/// <summary>
/// 小数保留位数
/// </summary>
[DisplayName("小数保留位数")]
[Range(0, 2, ErrorMessage = "小数保留位数必须在0-2之间")]
[AmisSelectField("小数保留位数", "0:不保留小数,1:保留1位小数,2:保留2位小数")]
public int ConversionDecimalPlaces { get; set; } = 1;

/// <summary>
/// 换算说明（自动生成，仅用于前端显示）
/// </summary>
[DisplayName("换算说明")]
public string ConversionDescription { get; private set; } = string.Empty;
```

## 3. 数据库迁移

### 3.1 扩展ExamRecord实体

**修改文件：`Data/Models/ExamRecord.cs`**
```csharp
// 在ExamRecord类中新增属性
/// <summary>
/// 原始成绩（换算前）
/// </summary>
[Range(0, 1000)]
public double? OriginalScore { get; set; }

/// <summary>
/// 换算后成绩（最终显示成绩）
/// </summary>
[Range(0, 1000)]
public double? ConvertedScore { get; set; }

/// <summary>
/// 是否应用了成绩换算
/// </summary>
public bool IsScoreConverted { get; set; } = false;

/// <summary>
/// 换算比例（记录当时的换算比例）
/// </summary>
public decimal? ScoreConversionRatio { get; set; }
```

### 3.2 生成数据库迁移

**执行EF迁移命令**：

1. **生成试卷换算字段迁移**：
```bash
# 在ExamApi项目目录下执行
dotnet ef migrations add AddScoreConversionFieldsToExamPaper

# 该命令将自动生成迁移文件，包含以下字段：
# - EnableScoreConversion (bit, nullable: false, default: false)
# - ConversionTargetFullScore (int, nullable: true)
# - ConversionTargetPassScore (int, nullable: true) 
# - ConversionDecimalPlaces (int, nullable: false, default: 1)
# - ConversionRatio (decimal(18,6), nullable: true)
# - IX_ExamPapers_EnableScoreConversion 索引
```

2. **生成考试记录换算字段迁移**：
```bash
# 在ExamApi项目目录下执行
dotnet ef migrations add AddScoreConversionFieldsToExamRecord

# 该命令将自动生成迁移文件，包含以下字段：
# - OriginalScore (float, nullable: true)
# - ConvertedScore (float, nullable: true)
# - IsScoreConverted (bit, nullable: false, default: false)
# - ScoreConversionRatio (decimal(18,6), nullable: true)
# - IX_ExamRecords_IsScoreConverted 索引
```

3. **应用迁移到数据库**：
```bash
# 更新数据库结构
dotnet ef database update

# 或者在开发环境中
dotnet ef database update --environment Development
```

4. **数据迁移脚本**（可选）：
```sql
-- 将现有的Score值复制到新字段（如果需要）
UPDATE ExamRecords 
SET OriginalScore = Score, 
    ConvertedScore = Score, 
    IsScoreConverted = 0 
WHERE Score IS NOT NULL;
```

## 4. 服务层实现

### 4.1 成绩换算服务接口

**新增文件：`Services/Interfaces/IScoreConversionService.cs`**
```csharp
using CodeSpirit.ExamApi.Dtos.ExamPaper;

namespace CodeSpirit.ExamApi.Services.Interfaces;

/// <summary>
/// 成绩换算服务接口
/// </summary>
public interface IScoreConversionService
{
    /// <summary>
    /// 计算换算后的成绩
    /// </summary>
    /// <param name="originalScore">原始成绩</param>
    /// <param name="originalFullScore">原始满分</param>
    /// <param name="targetFullScore">目标满分</param>
    /// <param name="decimalPlaces">小数保留位数</param>
    /// <returns>换算后的成绩</returns>
    double ConvertScore(double originalScore, int originalFullScore, int targetFullScore, int decimalPlaces = 1);

    /// <summary>
    /// 计算换算后的及格分数
    /// </summary>
    /// <param name="originalPassScore">原始及格分</param>
    /// <param name="originalFullScore">原始满分</param>
    /// <param name="targetFullScore">目标满分</param>
    /// <param name="decimalPlaces">小数保留位数</param>
    /// <returns>换算后的及格分数</returns>
    double ConvertPassScore(int originalPassScore, int originalFullScore, int targetFullScore, int decimalPlaces = 1);

    /// <summary>
    /// 验证换算规则的有效性
    /// </summary>
    /// <param name="originalFullScore">原始满分</param>
    /// <param name="targetFullScore">目标满分</param>
    /// <param name="targetPassScore">目标及格分</param>
    /// <param name="decimalPlaces">小数保留位数</param>
    /// <returns>验证结果</returns>
    (bool IsValid, string ErrorMessage) ValidateConversionRule(int originalFullScore, int targetFullScore, int targetPassScore, int decimalPlaces);

    /// <summary>
    /// 自动生成换算描述
    /// </summary>
    /// <param name="originalFullScore">原始满分</param>
    /// <param name="targetFullScore">目标满分</param>
    /// <param name="decimalPlaces">小数保留位数</param>
    /// <returns>换算描述</returns>
    string GenerateConversionDescription(int originalFullScore, int targetFullScore, int decimalPlaces);

    /// <summary>
    /// 计算换算比例
    /// </summary>
    /// <param name="originalFullScore">原始满分</param>
    /// <param name="targetFullScore">目标满分</param>
    /// <returns>换算比例</returns>
    decimal CalculateConversionRatio(int originalFullScore, int targetFullScore);
}
```

### 4.2 成绩换算服务实现

**新增文件：`Services/Implementations/ScoreConversionService.cs`**
```csharp
using CodeSpirit.Core;
using CodeSpirit.ExamApi.Services.Interfaces;

namespace CodeSpirit.ExamApi.Services.Implementations;

/// <summary>
/// 成绩换算服务实现
/// </summary>
public class ScoreConversionService : IScoreConversionService
{
    /// <summary>
    /// 计算换算后的成绩
    /// </summary>
    public double ConvertScore(double originalScore, int originalFullScore, int targetFullScore, int decimalPlaces = 1)
    {
        if (originalFullScore <= 0)
            throw new AppServiceException(400, "原始满分必须大于0");

        if (targetFullScore <= 0)
            throw new AppServiceException(400, "目标满分必须大于0");

        // 计算换算比例：目标满分 / 原始满分
        double conversionRatio = (double)targetFullScore / originalFullScore;
        
        // 换算成绩
        double convertedScore = originalScore * conversionRatio;
        
        // 保留指定的小数位数
        return Math.Round(convertedScore, decimalPlaces);
    }

    /// <summary>
    /// 计算换算后的及格分数
    /// </summary>
    public double ConvertPassScore(int originalPassScore, int originalFullScore, int targetFullScore, int decimalPlaces = 1)
    {
        return ConvertScore(originalPassScore, originalFullScore, targetFullScore, decimalPlaces);
    }

    /// <summary>
    /// 验证换算规则的有效性
    /// </summary>
    public (bool IsValid, string ErrorMessage) ValidateConversionRule(int originalFullScore, int targetFullScore, int targetPassScore, int decimalPlaces)
    {
        if (targetFullScore <= 0)
            return (false, "目标满分必须大于0");

        if (targetPassScore < 0)
            return (false, "目标及格分不能小于0");

        if (targetPassScore > targetFullScore)
            return (false, "目标及格分不能大于目标满分");

        if (decimalPlaces < 0 || decimalPlaces > 2)
            return (false, "小数保留位数必须在0-2之间");

        if (originalFullScore <= 0)
            return (false, "原始满分必须大于0");

        return (true, string.Empty);
    }

    /// <summary>
    /// 自动生成换算描述
    /// </summary>
    public string GenerateConversionDescription(int originalFullScore, int targetFullScore, int decimalPlaces)
    {
        var ratio = CalculateConversionRatio(originalFullScore, targetFullScore);
        var ratioDisplay = Math.Round((double)ratio, 3);

        return $"将{originalFullScore}分制转换为{targetFullScore}分制，" +
               $"换算比例为{targetFullScore}/{originalFullScore}={ratioDisplay}，" +
               $"小数保留{decimalPlaces}位";
    }

    /// <summary>
    /// 计算换算比例
    /// </summary>
    public decimal CalculateConversionRatio(int originalFullScore, int targetFullScore)
    {
        if (originalFullScore <= 0)
            throw new AppServiceException(400, "原始满分必须大于0");

        if (targetFullScore <= 0)
            throw new AppServiceException(400, "目标满分必须大于0");

        return (decimal)targetFullScore / originalFullScore;
    }
}
```

## 5. 修改现有服务

### 5.1 修改ExamPaperService

**修改文件：`Services/Implementations/ExamPaperService.cs`**

在`GenerateRandomExamPaperAsync`方法中添加成绩换算规则的保存：
```csharp
// 在GenerateRandomExamPaperAsync方法中，创建试卷时添加
if (createDto.EnableScoreConversion)
{
    examPaper.EnableScoreConversion = true;
    examPaper.ConversionTargetFullScore = createDto.ConversionTargetFullScore;
    examPaper.ConversionTargetPassScore = createDto.ConversionTargetPassScore;
    examPaper.ConversionDecimalPlaces = createDto.ConversionDecimalPlaces;
    examPaper.ConversionRatio = _scoreConversionService.CalculateConversionRatio(
        createDto.TotalScore, createDto.ConversionTargetFullScore);
}
```

在`ValidateRandomExamPaperRules`方法中添加换算规则验证：
```csharp
// 验证成绩换算规则
if (createDto.EnableScoreConversion)
{
    var (isValid, errorMessage) = _scoreConversionService.ValidateConversionRule(
        createDto.TotalScore,
        createDto.ConversionTargetFullScore,
        createDto.ConversionTargetPassScore,
        createDto.ConversionDecimalPlaces);
    
    if (!isValid)
    {
        throw new AppServiceException(400, $"成绩换算规则验证失败：{errorMessage}");
    }
}
```

### 5.2 修改ExamRecordService

**修改文件：`Services/Implementations/ExamRecordService.cs`**

在成绩计算相关方法中集成换算逻辑：
```csharp
// 在SubmitExamAsync和相关评分方法中添加成绩换算逻辑
private async Task ApplyScoreConversion(ExamRecord examRecord, double originalScore)
{
    var examPaper = examRecord.ExamSetting.ExamPaper;
    
    // 保存原始成绩
    examRecord.OriginalScore = originalScore;
    
    if (!examPaper.EnableScoreConversion || 
        examPaper.ConversionTargetFullScore == null ||
        examPaper.ConversionRatio == null)
    {
        // 未启用换算，原始成绩即为最终成绩
        examRecord.ConvertedScore = originalScore;
        examRecord.Score = originalScore;
        examRecord.IsScoreConverted = false;
        examRecord.ScoreConversionRatio = null;
    }
    else
    {
        // 应用换算规则
        var convertedScore = _scoreConversionService.ConvertScore(
            originalScore, 
            examPaper.TotalScore, 
            examPaper.ConversionTargetFullScore.Value,
            examPaper.ConversionDecimalPlaces);
        
        examRecord.ConvertedScore = convertedScore;
        examRecord.Score = convertedScore; // Score字段保持向后兼容，存储换算后成绩
        examRecord.IsScoreConverted = true;
        examRecord.ScoreConversionRatio = examPaper.ConversionRatio;
    }
    
    // 计算是否通过（基于换算后成绩）
    var passScore = CalculateConvertedPassScore(examPaper);
    examRecord.IsPassed = examRecord.ConvertedScore >= passScore;
}

private double CalculateConvertedPassScore(ExamPaper examPaper)
{
    if (!examPaper.EnableScoreConversion || 
        examPaper.ConversionTargetPassScore == null)
    {
        return examPaper.PassScore;
    }
    
    return examPaper.ConversionTargetPassScore.Value;
}

// 修改现有的评分方法，使用新的换算逻辑
// 例如在SubmitExamAsync方法中：
// 计算原始分数
double originalTotalScore = CalculateOriginalScore(examRecord);

// 应用成绩换算
await ApplyScoreConversion(examRecord, originalTotalScore);
```

## 6. DTO扩展

### 6.1 扩展ExamPaperDto

**修改文件：`Dtos/ExamPaper/ExamPaperDto.cs`**
```csharp
// 新增属性
/// <summary>
/// 是否启用成绩换算
/// </summary>
[DisplayName("启用成绩换算")]
public bool EnableScoreConversion { get; set; }

/// <summary>
/// 换算目标满分
/// </summary>
[DisplayName("换算目标满分")]
public int? ConversionTargetFullScore { get; set; }

/// <summary>
/// 换算目标及格分
/// </summary>
[DisplayName("换算目标及格分")]
public int? ConversionTargetPassScore { get; set; }

/// <summary>
/// 换算小数保留位数
/// </summary>
[DisplayName("小数保留位数")]
public int ConversionDecimalPlaces { get; set; }

/// <summary>
/// 换算比例
/// </summary>
[DisplayName("换算比例")]
public decimal? ConversionRatio { get; set; }

/// <summary>
/// 换算描述（自动生成，仅用于前端显示）
/// </summary>
[DisplayName("换算说明")]
public string ConversionDescription { get; set; } = string.Empty;

/// <summary>
/// 换算后的显示满分（用于前端显示）
/// </summary>
[DisplayName("显示满分")]
public int DisplayFullScore => EnableScoreConversion && ConversionTargetFullScore.HasValue 
    ? ConversionTargetFullScore.Value 
    : TotalScore;

/// <summary>
/// 换算后的显示及格分（用于前端显示）
/// </summary>
[DisplayName("显示及格分")]
public double DisplayPassScore => EnableScoreConversion && ConversionTargetPassScore.HasValue 
    ? ConversionTargetPassScore.Value 
    : PassScore;
```

### 6.2 扩展ExamRecordDto

**修改文件：`Dtos/ExamRecord/ExamRecordDto.cs`**（如果存在）
```csharp
// 新增属性
/// <summary>
/// 原始成绩
/// </summary>
[DisplayName("原始成绩")]
public double? OriginalScore { get; set; }

/// <summary>
/// 换算后成绩
/// </summary>
[DisplayName("换算后成绩")]
public double? ConvertedScore { get; set; }

/// <summary>
/// 是否应用了成绩换算
/// </summary>
[DisplayName("已应用换算")]
public bool IsScoreConverted { get; set; }
```

## 7. 依赖注入配置

**修改文件：`ServiceCollectionExtensions.cs`**
```csharp
// 在ConfigureServices方法中添加
services.AddScoped<IScoreConversionService, ScoreConversionService>();
```

## 8. 映射配置

**修改文件：`MappingProfiles/ExamPaperProfile.cs`**
```csharp
// 在映射配置中添加
CreateMap<GenerateRandomExamPaperDto, ExamPaper>()
    .ForMember(dest => dest.ConversionRatio, opt => opt.Ignore()); // 在服务层手动计算

// 从ExamPaper到ExamPaperDto的映射，自动生成换算描述
CreateMap<ExamPaper, ExamPaperDto>()
    .ForMember(dest => dest.ConversionDescription, opt => opt.MapFrom<ConversionDescriptionResolver>());

// 自定义解析器，用于自动生成换算描述
public class ConversionDescriptionResolver : IValueResolver<ExamPaper, ExamPaperDto, string>
{
    private readonly IScoreConversionService _scoreConversionService;

    public ConversionDescriptionResolver(IScoreConversionService scoreConversionService)
    {
        _scoreConversionService = scoreConversionService;
    }

    public string Resolve(ExamPaper source, ExamPaperDto destination, string destMember, ResolutionContext context)
    {
        if (!source.EnableScoreConversion || !source.ConversionTargetFullScore.HasValue)
        {
            return "未启用成绩换算";
        }

        return _scoreConversionService.GenerateConversionDescription(
            source.TotalScore,
            source.ConversionTargetFullScore.Value,
            source.ConversionDecimalPlaces);
    }
}
```

## 9. 前端界面配置

### 9.1 成绩换算规则表单配置

**Amis字段特性说明**：
- `AmisSwitchFieldAttribute` - 启用/禁用开关，支持OnText和OffText配置
- `AmisNumberFieldAttribute` - 数字输入框，支持Min、Max、Step、Precision等配置
- `AmisSelectFieldAttribute` - 下拉选择框，用于小数保留位数选择

**表单字段配置**：
```csharp
// 需要在GenerateRandomExamPaperDto.cs中添加using语句
using CodeSpirit.Amis.Attributes.FormFields;

// 字段配置说明：
[AmisSwitchField("启用成绩换算", false)] // 开关，默认关闭
public bool EnableScoreConversion { get; set; } = false;

[AmisNumberField("换算目标满分", Min = 1, Max = 1000, DefaultValue = 100)] // 数字输入，带范围限制
public int ConversionTargetFullScore { get; set; } = 100;

[AmisNumberField("换算目标及格分", Min = 0, Max = 1000, DefaultValue = 60)]
public int ConversionTargetPassScore { get; set; } = 60;

[AmisNumberField("小数保留位数", Min = 0, Max = 2, DefaultValue = 1)]
public int ConversionDecimalPlaces { get; set; } = 1;
```

### 9.2 成绩显示优化
- 在成绩列表中同时显示原始成绩和换算后成绩
- 在试卷详情中显示自动生成的换算规则说明
- 在考试结果中明确标识是否应用了成绩换算
- 换算描述在前端显示时实时生成，不存储在数据库中

## 10. 详细使用流程

### 10.1 管理员配置流程

**步骤1：创建支持换算的试卷**
```
管理员登录 → 试卷管理 → 生成随机试卷 → 配置成绩换算
```

**配置界面操作**：
1. 设置试卷基本信息（名称、总分、及格分、时长等）
2. 配置题型分布规则和难度分布
3. **启用成绩换算开关** - 开启换算功能
4. **设置换算目标满分** - 通常设为100分
5. **设置换算目标及格分** - 通常设为60分
6. **选择小数保留位数** - 0-2位小数
7. 系统自动生成换算描述预览
8. 保存试卷配置

**配置示例**：
```
原始试卷：120分满分，72分及格
换算配置：100分满分，60分及格，保留1位小数
系统显示：将120分制转换为100分制，换算比例为100/120=0.833，小数保留1位
```

### 10.2 学生考试流程

**步骤1：参加考试**
```
学生登录 → 选择考试 → 开始答题 → 提交试卷
```

**系统处理流程**：
1. 学生正常答题，界面显示原始分值
2. 提交后系统自动评分，计算原始成绩
3. 检查试卷是否启用成绩换算
4. 如启用，应用换算规则计算最终成绩
5. 同时保存原始成绩和换算后成绩

**成绩计算示例**：
```
学生答题得分：96分（原始成绩）
换算规则：100/120 = 0.833
最终成绩：96 × 0.833 = 80.0分
通过判断：80.0 ≥ 60分 → 通过
```

### 10.3 成绩查看流程

**学生端查看**：
```
学生登录 → 我的考试 → 查看成绩详情
```

**显示内容**：
- **最终成绩**：80.0分（主要显示）
- **原始成绩**：96分（如需透明度）
- **换算说明**：将120分制转换为100分制
- **通过状态**：已通过（基于换算后成绩）

**管理员端查看**：
```
管理员登录 → 成绩管理 → 查看考试统计
```

**统计数据**：
- 显示换算后的统计数据（平均分、通过率等）
- 可选择查看原始成绩统计
- 支持按换算状态筛选成绩记录

### 10.4 数据流转图

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│   试卷配置   │───▶│   学生答题   │───▶│   成绩计算   │
│启用换算规则  │    │获得原始分数  │    │应用换算公式  │
└─────────────┘    └─────────────┘    └─────────────┘
                                            │
                                            ▼
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│   成绩展示   │◀───│   数据存储   │◀───│   结果保存   │
│显示最终成绩  │    │双重成绩记录  │    │原始+换算成绩 │
└─────────────┘    └─────────────┘    └─────────────┘
```

### 10.5 典型使用场景

**场景1：职业资格考试**
```
需求：满足国家标准百分制要求
配置：150分试卷 → 100分制
换算：考生127.5分 → 85.0分（通过）
```

**场景2：课程结业考试**
```
需求：统一成绩标准
配置：120分试卷 → 100分制
换算：考生108分 → 90.0分（优秀）
```

**场景3：等级认定考试**
```
需求：多套试卷成绩可比
配置：不同分值试卷 → 统一100分制
换算：确保公平性和一致性
```

### 10.6 异常处理流程

**配置阶段异常**：
- 目标满分 ≤ 0：提示"目标满分必须大于0"
- 目标及格分 > 目标满分：提示"目标及格分不能大于目标满分"
- 换算比例异常：自动重新计算

**考试阶段异常**：
- 换算服务异常：使用原始成绩作为最终成绩
- 计算精度异常：按配置的小数位数四舍五入
- 数据保存异常：确保至少保存原始成绩

## 11. 测试用例

### 11.1 单元测试用例
```csharp
[Test]
public void ConvertScore_120To100_ShouldReturnCorrectScore()
{
    // 120分制转100分制：100分 -> 83.3分
    var result = _scoreConversionService.ConvertScore(100, 120, 100, 1);
    Assert.AreEqual(83.3, result);
}

[Test]
public void ConvertPassScore_120To100_ShouldReturnCorrectPassScore()
{
    // 120分制的72分及格线转为100分制的60分及格线
    var result = _scoreConversionService.ConvertPassScore(72, 120, 100, 0);
    Assert.AreEqual(60, result);
}

[Test]
public void GenerateConversionDescription_ShouldReturnCorrectDescription()
{
    // 测试自动生成换算描述
    var result = _scoreConversionService.GenerateConversionDescription(120, 100, 1);
    var expected = "将120分制转换为100分制，换算比例为100/120=0.833，小数保留1位";
    Assert.AreEqual(expected, result);
}

[Test]
public void ValidateConversionRule_ShouldReturnValidResult()
{
    // 测试换算规则验证
    var (isValid, errorMessage) = _scoreConversionService.ValidateConversionRule(120, 100, 60, 1);
    Assert.IsTrue(isValid);
    Assert.AreEqual(string.Empty, errorMessage);
}

[Test]
public void ValidateConversionRule_InvalidTargetFullScore_ShouldReturnError()
{
    // 测试无效的目标满分
    var (isValid, errorMessage) = _scoreConversionService.ValidateConversionRule(120, 0, 60, 1);
    Assert.IsFalse(isValid);
    Assert.AreEqual("目标满分必须大于0", errorMessage);
}

[Test]
public void CalculateConversionRatio_ShouldReturnCorrectRatio()
{
    // 测试换算比例计算
    var result = _scoreConversionService.CalculateConversionRatio(120, 100);
    Assert.AreEqual(0.833333m, Math.Round(result, 6));
}

[Test]
public void ApplyScoreConversion_WithConversion_ShouldSaveOriginalAndConvertedScores()
{
    // 测试成绩换算应用
    var examRecord = new ExamRecord();
    var examPaper = new ExamPaper
    {
        TotalScore = 120,
        PassScore = 72,
        EnableScoreConversion = true,
        ConversionTargetFullScore = 100,
        ConversionTargetPassScore = 60,
        ConversionDecimalPlaces = 1,
        ConversionRatio = 0.833333m
    };
    
    examRecord.ExamSetting = new ExamSetting { ExamPaper = examPaper };
    
    // 假设原始成绩为96分
    var originalScore = 96.0;
    
    // 应用换算逻辑（需要在测试中模拟服务调用）
    // 预期结果：原始96分 -> 换算后80.0分
    
    Assert.AreEqual(96.0, examRecord.OriginalScore);
    Assert.AreEqual(80.0, examRecord.ConvertedScore);
    Assert.IsTrue(examRecord.IsScoreConverted);
    Assert.AreEqual(0.833333m, examRecord.ScoreConversionRatio);
}
```

## 12. 实施步骤

1. **第一阶段：数据模型扩展**
   - 创建`ScoreConversionRule`DTO
   - 扩展`ExamPaper`实体
   - 生成并执行数据库迁移

2. **第二阶段：服务层实现**
   - 实现`IScoreConversionService`接口和服务
   - 修改`ExamPaperService`支持换算规则
   - 配置依赖注入

3. **第三阶段：成绩计算集成**
   - 修改`ExamRecordService`集成换算逻辑
   - 扩展相关DTO
   - 配置映射关系

4. **第四阶段：测试验证**
   - 编写单元测试
   - 进行集成测试
   - 验证前端界面

## 13. 风险控制

- **数据一致性**：确保换算前后的成绩逻辑一致
- **精度控制**：合理设置小数保留位数避免精度损失
- **向后兼容**：确保现有试卷不受影响
- **业务规则**：严格验证换算规则的合理性

## 14. 监控和日志

- 记录成绩换算的详细日志
- 监控换算规则的使用情况
- 建立异常换算结果的告警机制

## 15. 数据类型合理性分析

### 15.1 数据类型选择说明

**整型字段（int）**：
- `ConversionTargetFullScore` - 目标满分：通常为整数值（如100、120），使用int合理
- `ConversionTargetPassScore` - 目标及格分：通常为整数值（如60、72），使用int合理  
- `ConversionDecimalPlaces` - 小数保留位数：0-2的整数，使用int合理

**浮点型字段（double）**：
- `OriginalScore` - 原始成绩：可能包含小数，使用double合理
- `ConvertedScore` - 换算后成绩：可能包含小数，使用double合理
- `Score` - 最终成绩：向后兼容字段，使用double合理

**高精度小数（decimal）**：
- `ConversionRatio` - 换算比例：需要精确计算，避免浮点误差，使用decimal合理
- `ScoreConversionRatio` - 换算比例记录：同上，使用decimal合理

### 15.2 数据类型优势

- **避免精度丢失**：换算比例使用decimal确保计算精度
- **兼容性良好**：成绩字段使用double与现有系统保持一致
- **存储效率**：整数配置使用int，节省存储空间
- **查询性能**：合适的数据类型便于数据库索引和查询优化

## 16. 统一成绩显示工具类

### 16.1 成绩换算显示工具服务

**新增文件：`Services/Interfaces/IScoreDisplayService.cs`**
```csharp
namespace CodeSpirit.ExamApi.Services.Interfaces;

/// <summary>
/// 成绩显示服务接口
/// </summary>
public interface IScoreDisplayService
{
    /// <summary>
    /// 构建成绩显示信息
    /// </summary>
    /// <param name="originalScore">原始成绩</param>
    /// <param name="originalFullScore">原始满分</param>
    /// <param name="examPaper">试卷信息</param>
    /// <returns>成绩显示信息</returns>
    ScoreDisplayInfo BuildScoreDisplay(double originalScore, int originalFullScore, ExamPaperDto examPaper);
    
    /// <summary>
    /// 构建成绩显示信息（考试记录版本）
    /// </summary>
    /// <param name="examRecord">考试记录</param>
    /// <param name="examPaper">试卷信息</param>
    /// <returns>成绩显示信息</returns>
    ScoreDisplayInfo BuildScoreDisplay(ExamRecordDto examRecord, ExamPaperDto examPaper);
    
    /// <summary>
    /// 生成成绩显示HTML片段
    /// </summary>
    /// <param name="scoreDisplay">成绩显示信息</param>
    /// <param name="showDetails">是否显示详细信息</param>
    /// <returns>HTML片段</returns>
    string GenerateScoreHtml(ScoreDisplayInfo scoreDisplay, bool showDetails = true);
    
    /// <summary>
    /// 生成成绩显示的Amis配置
    /// </summary>
    /// <param name="scoreDisplay">成绩显示信息</param>
    /// <param name="cssClass">CSS类名</param>
    /// <returns>Amis配置对象</returns>
    JObject GenerateScoreAmisConfig(ScoreDisplayInfo scoreDisplay, string cssClass = "score-display");
}
```

**新增文件：`Models/ScoreDisplayInfo.cs`**
```csharp
namespace CodeSpirit.ExamApi.Models;

/// <summary>
/// 成绩显示信息
/// </summary>
public class ScoreDisplayInfo
{
    /// <summary>
    /// 原始成绩
    /// </summary>
    public double OriginalScore { get; set; }
    
    /// <summary>
    /// 原始满分
    /// </summary>
    public int OriginalFullScore { get; set; }
    
    /// <summary>
    /// 换算后成绩
    /// </summary>
    public double? ConvertedScore { get; set; }
    
    /// <summary>
    /// 换算后满分
    /// </summary>
    public int? ConvertedFullScore { get; set; }
    
    /// <summary>
    /// 是否启用了成绩换算
    /// </summary>
    public bool IsScoreConverted { get; set; }
    
    /// <summary>
    /// 换算比例
    /// </summary>
    public decimal? ConversionRatio { get; set; }
    
    /// <summary>
    /// 换算描述
    /// </summary>
    public string ConversionDescription { get; set; } = string.Empty;
    
    /// <summary>
    /// 显示成绩（优先显示换算后成绩）
    /// </summary>
    public double DisplayScore => ConvertedScore ?? OriginalScore;
    
    /// <summary>
    /// 显示满分（优先显示换算后满分）
    /// </summary>
    public int DisplayFullScore => ConvertedFullScore ?? OriginalFullScore;
    
    /// <summary>
    /// 得分率
    /// </summary>
    public double ScoreRate => DisplayFullScore > 0 ? (DisplayScore / DisplayFullScore) * 100 : 0;
    
    /// <summary>
    /// 成绩等级文本
    /// </summary>
    public string GradeText
    {
        get
        {
            var rate = ScoreRate;
            return rate switch
            {
                >= 90 => "优秀",
                >= 80 => "良好", 
                >= 70 => "中等",
                >= 60 => "及格",
                _ => "不及格"
            };
        }
    }
    
    /// <summary>
    /// 是否通过
    /// </summary>
    public bool IsPassed { get; set; }
}
```

**新增文件：`Services/Implementations/ScoreDisplayService.cs`**
```csharp
using CodeSpirit.ExamApi.Services.Interfaces;
using CodeSpirit.ExamApi.Models;
using CodeSpirit.ExamApi.Dtos.ExamPaper;
using CodeSpirit.ExamApi.Dtos.ExamRecord;
using Newtonsoft.Json.Linq;

namespace CodeSpirit.ExamApi.Services.Implementations;

/// <summary>
/// 成绩显示服务实现
/// </summary>
public class ScoreDisplayService : IScoreDisplayService
{
    private readonly IScoreConversionService _scoreConversionService;
    
    public ScoreDisplayService(IScoreConversionService scoreConversionService)
    {
        _scoreConversionService = scoreConversionService;
    }
    
    /// <summary>
    /// 构建成绩显示信息
    /// </summary>
    public ScoreDisplayInfo BuildScoreDisplay(double originalScore, int originalFullScore, ExamPaperDto examPaper)
    {
        var scoreDisplay = new ScoreDisplayInfo
        {
            OriginalScore = originalScore,
            OriginalFullScore = originalFullScore,
            IsScoreConverted = examPaper.EnableScoreConversion
        };
        
        if (examPaper.EnableScoreConversion && examPaper.ConversionTargetFullScore.HasValue)
        {
            scoreDisplay.ConvertedScore = _scoreConversionService.ConvertScore(
                originalScore,
                originalFullScore,
                examPaper.ConversionTargetFullScore.Value,
                examPaper.ConversionDecimalPlaces);
            scoreDisplay.ConvertedFullScore = examPaper.ConversionTargetFullScore.Value;
            scoreDisplay.ConversionRatio = examPaper.ConversionRatio;
            scoreDisplay.ConversionDescription = _scoreConversionService.GenerateConversionDescription(
                originalFullScore,
                examPaper.ConversionTargetFullScore.Value,
                examPaper.ConversionDecimalPlaces);
            
            // 基于换算后成绩判断是否通过
            var convertedPassScore = examPaper.ConversionTargetPassScore ?? 60;
            scoreDisplay.IsPassed = scoreDisplay.ConvertedScore >= convertedPassScore;
        }
        else
        {
            // 基于原始成绩判断是否通过
            scoreDisplay.IsPassed = originalScore >= examPaper.PassScore;
        }
        
        return scoreDisplay;
    }
    
    /// <summary>
    /// 构建成绩显示信息（考试记录版本）
    /// </summary>
    public ScoreDisplayInfo BuildScoreDisplay(ExamRecordDto examRecord, ExamPaperDto examPaper)
    {
        var scoreDisplay = new ScoreDisplayInfo
        {
            OriginalScore = examRecord.OriginalScore ?? examRecord.Score ?? 0,
            OriginalFullScore = examPaper.TotalScore,
            IsScoreConverted = examRecord.IsScoreConverted,
            IsPassed = examRecord.IsPassed
        };
        
        if (examRecord.IsScoreConverted && examRecord.ConvertedScore.HasValue)
        {
            scoreDisplay.ConvertedScore = examRecord.ConvertedScore.Value;
            scoreDisplay.ConvertedFullScore = examPaper.ConversionTargetFullScore;
            scoreDisplay.ConversionRatio = examRecord.ScoreConversionRatio;
            
            if (examPaper.ConversionTargetFullScore.HasValue)
            {
                scoreDisplay.ConversionDescription = _scoreConversionService.GenerateConversionDescription(
                    examPaper.TotalScore,
                    examPaper.ConversionTargetFullScore.Value,
                    examPaper.ConversionDecimalPlaces);
            }
        }
        
        return scoreDisplay;
    }
    
    /// <summary>
    /// 生成成绩显示HTML片段
    /// </summary>
    public string GenerateScoreHtml(ScoreDisplayInfo scoreDisplay, bool showDetails = true)
    {
        var html = new StringBuilder();
        
        if (scoreDisplay.IsScoreConverted)
        {
            // 显示换算后成绩（主要显示）
            html.AppendLine($"<div class=\"score-display converted\">");
            html.AppendLine($"  <div class=\"main-score\">");
            html.AppendLine($"    <span class=\"score-value\">{scoreDisplay.ConvertedScore:F1}</span>");
            html.AppendLine($"    <span class=\"score-unit\">/{scoreDisplay.ConvertedFullScore}分</span>");
            html.AppendLine($"    <span class=\"score-badge converted\">已换算</span>");
            html.AppendLine($"  </div>");
            
            if (showDetails)
            {
                html.AppendLine($"  <div class=\"original-score\">");
                html.AppendLine($"    <small>原始成绩：{scoreDisplay.OriginalScore:F1}/{scoreDisplay.OriginalFullScore}分</small>");
                html.AppendLine($"  </div>");
                html.AppendLine($"  <div class=\"conversion-note\">");
                html.AppendLine($"    <small class=\"text-muted\">{scoreDisplay.ConversionDescription}</small>");
                html.AppendLine($"  </div>");
            }
            
            html.AppendLine($"</div>");
        }
        else
        {
            // 显示原始成绩
            html.AppendLine($"<div class=\"score-display original\">");
            html.AppendLine($"  <div class=\"main-score\">");
            html.AppendLine($"    <span class=\"score-value\">{scoreDisplay.OriginalScore:F1}</span>");
            html.AppendLine($"    <span class=\"score-unit\">/{scoreDisplay.OriginalFullScore}分</span>");
            html.AppendLine($"  </div>");
            html.AppendLine($"</div>");
        }
        
        return html.ToString();
    }
    
    /// <summary>
    /// 生成成绩显示的Amis配置
    /// </summary>
    public JObject GenerateScoreAmisConfig(ScoreDisplayInfo scoreDisplay, string cssClass = "score-display")
    {
        if (scoreDisplay.IsScoreConverted)
        {
            return new JObject
            {
                ["type"] = "html",
                ["className"] = $"{cssClass} converted",
                ["html"] = $@"
                    <div class=""score-main"">
                        <div class=""score-value"">{scoreDisplay.ConvertedScore:F1}</div>
                        <div class=""score-unit"">/ {scoreDisplay.ConvertedFullScore}分</div>
                        <div class=""score-badge converted"">已换算</div>
                    </div>
                    <div class=""score-original"">
                        <small>原始：{scoreDisplay.OriginalScore:F1}/{scoreDisplay.OriginalFullScore}分</small>
                    </div>
                    <div class=""score-note"">
                        <small class=""text-muted"">{scoreDisplay.ConversionDescription}</small>
                    </div>
                "
            };
        }
        else
        {
            return new JObject
            {
                ["type"] = "html", 
                ["className"] = $"{cssClass} original",
                ["html"] = $@"
                    <div class=""score-main"">
                        <div class=""score-value"">{scoreDisplay.OriginalScore:F1}</div>
                        <div class=""score-unit"">/ {scoreDisplay.OriginalFullScore}分</div>
                    </div>
                "
            };
        }
    }
}
```

## 17. 前端结果页面修改方案

### 17.1 JavaScript结果页面增强 (result.js)

**修改文件：`CodeSpirit.Web/wwwroot/js/exam/result.js`**

在`buildResultOverview`方法中增加成绩换算显示支持：

```javascript
/**
 * 构建成绩概览（支持成绩换算显示）
 */
buildResultOverview() {
    // 检查是否启用了成绩换算
    const isScoreConverted = this.resultData.isScoreConverted || false;
    const originalScore = this.resultData.originalScore || this.resultData.score;
    const convertedScore = this.resultData.convertedScore;
    const originalTotalScore = this.examData.totalScore || this.resultData.totalScore;
    const convertedTotalScore = this.examData.conversionTargetFullScore;
    
    // 确定显示的主要成绩
    const displayScore = isScoreConverted ? convertedScore : originalScore;
    const displayTotalScore = isScoreConverted ? convertedTotalScore : originalTotalScore;
    
    const scorePercentage = displayTotalScore > 0 
        ? ((displayScore / displayTotalScore) * 100).toFixed(1)
        : 0;

    // 构建成绩卡片
    const scoreCard = this.buildScoreCard(isScoreConverted, originalScore, convertedScore, 
                                         originalTotalScore, convertedTotalScore);

    return {
        type: 'grid',
        className: 'result-overview',
        columns: [
            scoreCard,
            {
                body: {
                    type: 'panel',
                    className: 'result-score-card',
                    body: {
                        type: 'html',
                        html: `
                            <div class="result-score-label">得分率</div>
                            <div class="result-score-value">${scorePercentage}%</div>
                            <div class="result-score-unit">百分比</div>
                        `
                    }
                }
            },
            {
                body: {
                    type: 'panel',
                    className: 'result-score-card',
                    body: {
                        type: 'html',
                        html: `
                            <div class="result-score-label">用时</div>
                            <div class="result-score-value">${this.formatDuration(this.resultData.duration || 0)}</div>
                            <div class="result-score-unit">时间</div>
                        `
                    }
                }
            },
            {
                body: {
                    type: 'panel',
                    className: 'result-score-card',
                    body: {
                        type: 'html',
                        html: `
                            <div class="result-score-label">状态</div>
                            <div class="result-score-value ${this.resultData.isPassed ? 'passed' : 'failed'}">
                                ${this.resultData.isPassed ? '通过' : '未通过'}
                            </div>
                            <div class="result-score-unit">结果</div>
                        `
                    }
                }
            }
        ]
    };
}

/**
 * 构建成绩卡片（支持换算显示）
 */
buildScoreCard(isScoreConverted, originalScore, convertedScore, originalTotalScore, convertedTotalScore) {
    if (isScoreConverted) {
        // 显示换算后成绩，并提供原始成绩提示
        return {
            body: {
                type: 'panel',
                className: 'result-score-card converted',
                body: {
                    type: 'html',
                    html: `
                        <div class="result-score-label">总分</div>
                        <div class="result-score-value score converted">${convertedScore}</div>
                        <div class="result-score-unit">/ ${convertedTotalScore}分 <span class="score-badge">已换算</span></div>
                        <div class="result-score-original">
                            <small>原始成绩：${originalScore}/${originalTotalScore}分</small>
                        </div>
                        <div class="result-score-note">
                            <small class="text-muted">${this.examData.conversionDescription || ''}</small>
                        </div>
                    `
                }
            }
        };
    } else {
        // 显示原始成绩
        return {
            body: {
                type: 'panel',
                className: 'result-score-card',
                body: {
                    type: 'html',
                    html: `
                        <div class="result-score-label">总分</div>
                        <div class="result-score-value score">${originalScore}</div>
                        <div class="result-score-unit">/ ${originalTotalScore}分</div>
                    `
                }
            }
        };
    }
}
```

**在CSS中添加成绩换算样式：**

```css
/* 成绩换算相关样式 */
.result-score-card.converted {
    border: 2px solid #52c41a;
    background: linear-gradient(135deg, #f6ffed 0%, #ffffff 100%);
}

.score-badge {
    display: inline-block;
    background: #52c41a;
    color: white;
    font-size: 10px;
    padding: 2px 6px;
    border-radius: 3px;
    margin-left: 5px;
}

.result-score-original {
    font-size: 12px;
    color: #666;
    margin-top: 5px;
}

.result-score-note {
    font-size: 11px;
    color: #999;
    margin-top: 3px;
    line-height: 1.2;
}

.score.converted {
    color: #52c41a;
    font-weight: bold;
}
```

## 18. 试卷预览修改方案

### 18.1 试卷预览配置增强

**修改文件：`Controllers/ExamPapersController.cs` 中的 `GetExamQuestionsPreviewConfig` 方法：**

```csharp
[HttpGet("{id}/questions-preview")]
[DisplayName("获取试卷题目预览配置")]
public async Task<ActionResult<ApiResponse<JObject>>> GetExamQuestionsPreviewConfig(long id)
{
    var examPaper = await _examPaperService.GetAsync(id);
    if (examPaper == null)
    {
        return NotFound("试卷不存在");
    }

    // 构建成绩显示信息
    var scoreDisplay = _scoreDisplayService.BuildScoreDisplay(examPaper.TotalScore, examPaper.TotalScore, examPaper);

    // 使用JObject/JArray构建表单
    var formItems = new JArray();

    // 添加试卷信息头部，包含成绩换算提示
    var headerInfo = new JObject
    {
        ["type"] = "panel",
        ["className"] = "exam-paper-header",
        ["body"] = new JArray()
    };

    var headerBody = (JArray)headerInfo["body"];

    // 基本信息
    headerBody.Add(new JObject
    {
        ["type"] = "html",
        ["html"] = $@"
            <div class=""exam-header-info"">
                <h3>{examPaper.Name}</h3>
                <div class=""exam-basic-info"">
                    <span>总分：{examPaper.TotalScore}分</span> | 
                    <span>及格分：{examPaper.PassScore}分</span> | 
                    <span>时长：{examPaper.Duration}分钟</span>
                </div>
            </div>
        "
    });

    // 成绩换算提醒
    if (examPaper.EnableScoreConversion)
    {
        headerBody.Add(new JObject
        {
            ["type"] = "alert",
            ["level"] = "info",
            ["className"] = "score-conversion-alert",
            ["body"] = $@"
                <div class=""conversion-reminder"">
                    <strong>⚠️ 成绩换算提醒</strong><br/>
                    本试卷启用了成绩换算功能：{scoreDisplay.ConversionDescription}<br/>
                    考生最终成绩将按照换算规则自动转换为<strong>{examPaper.ConversionTargetFullScore}分制</strong>显示。
                </div>
            "
        });
    }

    formItems.Add(headerInfo);

    // 为每个题目创建对应的表单组件
    for (int i = 0; i < examPaper.Questions.Count; i++)
    {
        var question = examPaper.Questions[i];
        question.Content = WebUtility.HtmlDecode(question.Content);
        question.Options = question.Options.Select(p => WebUtility.HtmlDecode(p)).ToList();
        question.CorrectAnswer = WebUtility.HtmlDecode(question.CorrectAnswer);
        question.Analysis = WebUtility.HtmlDecode(question.Analysis ?? string.Empty);
        int index = i + 1;

        // 题目分数显示（原始分数）
        var scoreText = examPaper.EnableScoreConversion 
            ? $"{question.Score}分（原始分值）" 
            : $"{question.Score}分";

        // 问题标题
        var titleObj = new JObject
        {
            ["type"] = "tpl",
            ["tpl"] = $"<div class=\"question-label\"><pre>{index}. ${{content | raw}} </pre><span style=\"color:#999\">（{scoreText}）</span></div>",
            ["inline"] = false
        };

        formItems.Add(titleObj);

        // 根据题目类型添加相应的预览组件...
        // [其余题目类型处理逻辑保持不变]
    }

    // 底部成绩换算说明
    if (examPaper.EnableScoreConversion)
    {
        formItems.Add(new JObject
        {
            ["type"] = "panel",
            ["className"] = "conversion-summary",
            ["body"] = new JObject
            {
                ["type"] = "html",
                ["html"] = $@"
                    <div class=""conversion-summary-content"">
                        <h4>📊 成绩换算规则</h4>
                        <div class=""conversion-details"">
                            <div class=""conversion-item"">
                                <span class=""label"">原始分制：</span>
                                <span class=""value"">{examPaper.TotalScore}分（满分）/ {examPaper.PassScore}分（及格）</span>
                            </div>
                            <div class=""conversion-item"">
                                <span class=""label"">目标分制：</span>
                                <span class=""value"">{examPaper.ConversionTargetFullScore}分（满分）/ {examPaper.ConversionTargetPassScore}分（及格）</span>
                            </div>
                            <div class=""conversion-item"">
                                <span class=""label"">换算比例：</span>
                                <span class=""value"">{examPaper.ConversionRatio:F4}</span>
                            </div>
                            <div class=""conversion-item"">
                                <span class=""label"">小数位数：</span>
                                <span class=""value"">保留{examPaper.ConversionDecimalPlaces}位小数</span>
                            </div>
                        </div>
                        <div class=""conversion-example"">
                            <strong>换算示例：</strong>假设考生得分90分，换算后成绩为 90 × {examPaper.ConversionRatio:F4} = {90 * (double)examPaper.ConversionRatio.Value:F1}分
                        </div>
                    </div>
                "
            }
        });
    }

    var form = new JObject
    {
        ["type"] = "form",
        ["title"] = "试卷预览",
        ["body"] = formItems,
        ["actions"] = new JArray()
    };

    return SuccessResponse(form);
}
```

## 19. 答卷预览修改方案

### 19.1 答卷预览成绩显示增强

**修改文件：`Controllers/ExamRecordsController.cs` 中的 `GetExamQuestionsPreviewConfig` 方法：**

```csharp
[HttpGet("{id}/questions-preview")]
[DisplayName("获取试卷题目预览配置")]
public async Task<ActionResult<ApiResponse<JObject>>> GetExamQuestionsPreviewConfig(long id)
{
    var preview = await _examRecordService.GetAnswerPreviewAsync(id);
    var examPaper = await _examPaperService.GetAsync(preview.ExamPaperId);
    
    if (examPaper == null)
    {
        return NotFound("试卷不存在");
    }

    // 构建成绩显示信息
    var scoreDisplay = _scoreDisplayService.BuildScoreDisplay(preview.StudentScore ?? 0, examPaper.TotalScore, examPaper);

    // 创建Amis页面
    var page = new JObject
    {
        ["type"] = "page",
        ["title"] = "答卷预览",
        ["body"] = new JArray()
    };

    // 添加头部统计信息（增强版本）
    var header = new JObject
    {
        ["type"] = "card",
        ["header"] = new JObject
        {
            ["title"] = "成绩统计",
            ["subTitle"] = scoreDisplay.IsScoreConverted 
                ? $"最终成绩: {scoreDisplay.ConvertedScore:F1}/{scoreDisplay.ConvertedFullScore}分 (已换算)"
                : $"成绩: {scoreDisplay.OriginalScore:F1}/{scoreDisplay.OriginalFullScore}分"
        },
        ["body"] = new JArray()
    };

    var headerBody = (JArray)header["body"];

    // 成绩换算提醒框
    if (scoreDisplay.IsScoreConverted)
    {
        headerBody.Add(new JObject
        {
            ["type"] = "alert",
            ["level"] = "success",
            ["className"] = "score-conversion-notice",
            ["body"] = $@"
                <div class=""score-conversion-info"">
                    <div class=""conversion-title"">✅ 已应用成绩换算</div>
                    <div class=""conversion-details"">
                        <div class=""score-comparison"">
                            <div class=""original-score"">
                                原始成绩：<strong>{scoreDisplay.OriginalScore:F1}/{scoreDisplay.OriginalFullScore}分</strong>
                            </div>
                            <div class=""conversion-arrow"">↓ 换算</div>
                            <div class=""converted-score"">
                                最终成绩：<strong style=""color: #52c41a"">{scoreDisplay.ConvertedScore:F1}/{scoreDisplay.ConvertedFullScore}分</strong>
                            </div>
                        </div>
                        <div class=""conversion-desc"">
                            <small>{scoreDisplay.ConversionDescription}</small>
                        </div>
                    </div>
                </div>
            "
        });
    }

    // 按题目类型分组显示得分（支持换算显示）
    var questionsByType = examPaper.Questions
        .GroupBy(q => q.Type.ToString())
        .ToDictionary(g => g.Key, g => g.ToList());

    var scoreByType = new Dictionary<string, (double Score, int TotalScore)>();
    foreach (var type in questionsByType.Keys)
    {
        var typeQuestions = questionsByType[type];
        var typeAnswers = preview.Answers.Where(a => a.QuestionType == type).ToList();

        double typeScore = typeAnswers.Sum(a => a.Score ?? 0);
        int typeTotalScore = typeQuestions.Sum(q => q.Score);

        scoreByType[type] = (typeScore, typeTotalScore);
    }

    // 题型得分表格
    var typeScoreTable = new JObject
    {
        ["type"] = "table",
        ["columns"] = new JArray(),
        ["items"] = new JArray()
    };

    var tableColumns = (JArray)typeScoreTable["columns"];
    tableColumns.Add(new JObject { ["label"] = "题型", ["name"] = "type" });

    if (scoreDisplay.IsScoreConverted)
    {
        tableColumns.Add(new JObject
        {
            ["label"] = "原始得分/总分",
            ["name"] = "originalScore",
            ["type"] = "tpl",
            ["tpl"] = "<span class=\"text-muted\">${originalScore}/${originalTotalScore}</span>"
        });
        tableColumns.Add(new JObject
        {
            ["label"] = "换算得分/总分",
            ["name"] = "convertedScore",
            ["type"] = "tpl", 
            ["tpl"] = "<span class=\"${scoreClass}\">${convertedScore}/${convertedTotalScore}</span>"
        });
    }
    else
    {
        tableColumns.Add(new JObject
        {
            ["label"] = "得分/总分",
            ["name"] = "score",
            ["type"] = "tpl",
            ["tpl"] = "<span class=\"${scoreClass}\">${score}/${totalScore}</span>"
        });
    }

    var tableItems = (JArray)typeScoreTable["items"];
    foreach (var typeScore in scoreByType)
    {
        string typeName = GetQuestionTypeName(typeScore.Key);
        bool isPassed = typeScore.Value.Score >= typeScore.Value.TotalScore * 0.6;

        var item = new JObject
        {
            ["type"] = typeName,
            ["originalScore"] = typeScore.Value.Score,
            ["originalTotalScore"] = typeScore.Value.TotalScore,
            ["scoreClass"] = isPassed ? "text-success" : "text-danger"
        };

        if (scoreDisplay.IsScoreConverted)
        {
            // 计算题型换算分数
            var convertedTypeScore = _scoreConversionService.ConvertScore(
                typeScore.Value.Score,
                examPaper.TotalScore,
                scoreDisplay.ConvertedFullScore.Value,
                examPaper.ConversionDecimalPlaces);
            var convertedTypeTotalScore = _scoreConversionService.ConvertScore(
                typeScore.Value.TotalScore,
                examPaper.TotalScore,
                scoreDisplay.ConvertedFullScore.Value,
                examPaper.ConversionDecimalPlaces);

            item["convertedScore"] = convertedTypeScore;
            item["convertedTotalScore"] = convertedTypeTotalScore;
        }
        else
        {
            item["score"] = typeScore.Value.Score;
            item["totalScore"] = typeScore.Value.TotalScore;
        }

        tableItems.Add(item);
    }

    headerBody.Add(typeScoreTable);
    ((JArray)page["body"]).Add(header);

    // 继续添加题目详细内容...
    // [其余题目显示逻辑保持不变，但需要在题目分数显示中也考虑换算]

    return SuccessResponse(page);
}
```

## 20. HTML导出修改方案

### 20.1 PDF导出成绩显示增强

**修改文件：`Controllers/ExamRecordsController.cs` 中的 `GenerateHtmlContent` 方法：**

```csharp
private async Task<string> GenerateHtmlContent(ExamPaperDetailDto record, ExamPaperDto examPaper, ExamPaperExportSettingsDto exportSettings)
{
    // 构建成绩显示信息
    var scoreDisplay = _scoreDisplayService.BuildScoreDisplay(record, examPaper);
    
    StringBuilder htmlBuilder = new StringBuilder();
    
    // HTML头部和样式 [保持现有代码]
    htmlBuilder.AppendLine("<!DOCTYPE html>");
    htmlBuilder.AppendLine("<html lang=\"zh-CN\">");
    htmlBuilder.AppendLine("<head>");
    htmlBuilder.AppendLine("  <meta charset=\"UTF-8\">");
    htmlBuilder.AppendLine("  <title>考试答卷</title>");
    htmlBuilder.AppendLine("  <style>");
    htmlBuilder.AppendLine("    body { font-family: 'Microsoft YaHei', Arial, sans-serif; margin: 20px; }");
    
    // 添加成绩换算相关样式
    htmlBuilder.AppendLine("    .score-conversion-notice { background: #f6ffed; border: 1px solid #b7eb8f; padding: 10px; margin: 10px 0; border-radius: 4px; }");
    htmlBuilder.AppendLine("    .score-comparison { display: flex; align-items: center; margin: 5px 0; }");
    htmlBuilder.AppendLine("    .score-comparison > div { margin: 0 10px; }");
    htmlBuilder.AppendLine("    .conversion-arrow { color: #52c41a; font-weight: bold; }");
    htmlBuilder.AppendLine("    .converted-score { color: #52c41a; }");
    htmlBuilder.AppendLine("    .score-badge { background: #52c41a; color: white; padding: 2px 6px; border-radius: 3px; font-size: 12px; }");
    
    // [其余样式保持不变]
    htmlBuilder.AppendLine("  </style>");
    htmlBuilder.AppendLine("</head>");
    htmlBuilder.AppendLine("<body>");

    // 页面头部（修改为支持成绩换算）
    htmlBuilder.AppendLine("  <div class=\"header\">");
    htmlBuilder.AppendLine($"    <div class=\"title\">{examPaper.Name ?? "考试答卷"}</div>");
    htmlBuilder.AppendLine("    <div class=\"info\">");

    var studentName = record.StudentName ?? "未知学生";
    var idNo = record.IdNo ?? "未设置";
    var admissionTicket = record.AdmissionTicket ?? "未设置";

    htmlBuilder.AppendLine($"      考生姓名: {studentName} | ");
    htmlBuilder.AppendLine($"      身份证号: {idNo} | ");
    htmlBuilder.AppendLine($"      准考证号: {admissionTicket}");
    htmlBuilder.AppendLine("    </div>");
    
    // 成绩信息（支持换算显示）
    htmlBuilder.AppendLine("    <div class=\"info\">");
    if (scoreDisplay.IsScoreConverted)
    {
        htmlBuilder.AppendLine($"      最终得分: <strong>{scoreDisplay.ConvertedScore:F1}/{scoreDisplay.ConvertedFullScore}分</strong> <span class=\"score-badge\">已换算</span> | ");
        htmlBuilder.AppendLine($"      原始得分: {scoreDisplay.OriginalScore:F1}/{scoreDisplay.OriginalFullScore}分 | ");
    }
    else
    {
        htmlBuilder.AppendLine($"      得分: {scoreDisplay.OriginalScore:F1}/{scoreDisplay.OriginalFullScore}分 | ");
    }

    // 时间信息 [保持现有代码]
    var submitTime = record.SubmitTime;
    var startTime = record.StartTime;
    var duration = record.Duration;

    var localStartTime = TimeZoneInfo.ConvertTimeFromUtc(startTime, TimeZoneInfo.Local);
    var localSubmitTime = submitTime.HasValue 
        ? TimeZoneInfo.ConvertTimeFromUtc(submitTime.Value, TimeZoneInfo.Local) 
        : (DateTime?)null;

    htmlBuilder.AppendLine($"      开始作答时间: {localStartTime.ToString("yyyy-MM-dd HH:mm:ss")} | ");
    htmlBuilder.AppendLine($"      提交时间: {localSubmitTime?.ToString("yyyy-MM-dd HH:mm:ss") ?? "未提交"} | ");
    
    if (duration.HasValue)
    {
        htmlBuilder.AppendLine($"      作答时长: {duration.Value}分钟");
    }
    else if (localSubmitTime.HasValue)
    {
        var actualDuration = (int)Math.Ceiling((localSubmitTime.Value - localStartTime).TotalMinutes);
        htmlBuilder.AppendLine($"      作答时长: {actualDuration}分钟");
    }
    else
    {
        htmlBuilder.AppendLine("      作答时长: 未完成");
    }
    
    htmlBuilder.AppendLine("    </div>");
    htmlBuilder.AppendLine("  </div>");

    // 成绩换算提醒
    if (scoreDisplay.IsScoreConverted)
    {
        htmlBuilder.AppendLine("  <div class=\"score-conversion-notice\">");
        htmlBuilder.AppendLine("    <div style=\"font-weight: bold; margin-bottom: 8px;\">📊 成绩换算说明</div>");
        htmlBuilder.AppendLine("    <div class=\"score-comparison\">");
        htmlBuilder.AppendLine($"      <div>原始成绩：{scoreDisplay.OriginalScore:F1}/{scoreDisplay.OriginalFullScore}分</div>");
        htmlBuilder.AppendLine("      <div class=\"conversion-arrow\">→</div>");
        htmlBuilder.AppendLine($"      <div class=\"converted-score\">最终成绩：{scoreDisplay.ConvertedScore:F1}/{scoreDisplay.ConvertedFullScore}分</div>");
        htmlBuilder.AppendLine("    </div>");
        htmlBuilder.AppendLine($"    <div style=\"font-size: 12px; color: #666; margin-top: 5px;\">{scoreDisplay.ConversionDescription}</div>");
        htmlBuilder.AppendLine("  </div>");
    }

    // 成绩统计表（修改为支持换算显示）
    htmlBuilder.AppendLine("  <div class=\"summary\">");
    htmlBuilder.AppendLine("    <div class=\"summary-title\">成绩统计</div>");
    htmlBuilder.AppendLine("    <table class=\"summary-table\">");
    htmlBuilder.AppendLine("      <tr>");
    htmlBuilder.AppendLine("        <th>题型</th>");
    htmlBuilder.AppendLine("        <th>题目数</th>");
    
    if (scoreDisplay.IsScoreConverted)
    {
        htmlBuilder.AppendLine("        <th>原始得分/总分</th>");
        htmlBuilder.AppendLine("        <th>换算得分/总分</th>");
    }
    else
    {
        htmlBuilder.AppendLine("        <th>得分/总分</th>");
    }
    
    htmlBuilder.AppendLine("      </tr>");

    // 按题型统计得分（支持换算显示）
    var questionsByType = examPaper.Questions
        .GroupBy(q => q.Type.ToString())
        .ToDictionary(g => g.Key, g => g.ToList());

    foreach (var type in questionsByType.Keys)
    {
        var typeQuestions = questionsByType[type];
        var typeAnswers = record.Answers.Where(a => a.QuestionType == type).ToList();

        double typeScore = typeAnswers.Sum(a => a.Score ?? 0);
        int typeTotalScore = typeQuestions.Sum(q => q.Score);
        string typeName = GetQuestionTypeName(type);

        htmlBuilder.AppendLine("      <tr>");
        htmlBuilder.AppendLine($"        <td>{typeName}</td>");
        htmlBuilder.AppendLine($"        <td>{typeQuestions.Count}</td>");
        
        if (scoreDisplay.IsScoreConverted)
        {
            // 计算题型换算分数
            var convertedTypeScore = _scoreConversionService.ConvertScore(
                typeScore, examPaper.TotalScore, scoreDisplay.ConvertedFullScore.Value, examPaper.ConversionDecimalPlaces);
            var convertedTypeTotalScore = _scoreConversionService.ConvertScore(
                typeTotalScore, examPaper.TotalScore, scoreDisplay.ConvertedFullScore.Value, examPaper.ConversionDecimalPlaces);
                
            htmlBuilder.AppendLine($"        <td style=\"color: #666;\">{typeScore:F1}/{typeTotalScore}</td>");
            htmlBuilder.AppendLine($"        <td style=\"color: #52c41a; font-weight: bold;\">{convertedTypeScore:F1}/{convertedTypeTotalScore:F1}</td>");
        }
        else
        {
            htmlBuilder.AppendLine($"        <td>{typeScore:F1}/{typeTotalScore}</td>");
        }
        
        htmlBuilder.AppendLine("      </tr>");
    }

    htmlBuilder.AppendLine("    </table>");
    htmlBuilder.AppendLine("  </div>");

    // [其余题目内容显示代码保持不变]

    htmlBuilder.AppendLine("</body>");
    htmlBuilder.AppendLine("</html>");

    return htmlBuilder.ToString();
}
```

## 21. 方案总结与优势

### 21.1 主要特点

1. **统一的成绩显示服务**：`ScoreDisplayService` 提供一致的成绩换算显示逻辑
2. **向后兼容性**：现有功能不受影响，成绩换算为可选功能
3. **醒目提醒**：在所有相关界面都有明显的换算状态提示
4. **双重显示**：同时显示原始成绩和换算后成绩，保证透明度
5. **代码复用**：统一的工具类避免重复代码和潜在错误

### 21.2 技术优势

- **查询性能**：使用具体字段便于建立索引和高效查询
- **数据一致性**：自动生成描述和比例，避免人为错误
- **向后兼容**：现有的Score字段继续存储最终成绩（换算后）
- **完整审计**：完整保留原始成绩和换算过程信息
- **组件复用**：使用成熟的Amis组件，确保UI一致性
- **统一封装**：通过服务层统一处理，减少出错概率

### 21.3 业务价值

- **合规性**：满足国家职业标准对百分制成绩的要求
- **透明性**：清晰展示换算规则和过程
- **灵活性**：支持任意分值到百分制的转换
- **可追溯性**：完整保留成绩换算的历史记录
- **用户体验**：统一的表单界面，操作简单直观 