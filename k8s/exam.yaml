apiVersion: apps/v1
kind: Deployment
metadata:
  name: exam
  namespace: code-spirit
  labels:
    k8s-app: exam
    qcloud-app: exam
spec:
  replicas: 1
  selector:
    matchLabels:
      k8s-app: exam
      qcloud-app: exam
  template:
    metadata:
      labels:
        k8s-app: exam
        qcloud-app: exam
    spec:
      containers:
      - name: exam
        image: ccr.ccs.tencentyun.com/codespirit/exam-api:master
        resources:
          requests:
            cpu: 10m
            memory: 256Mi
        volumeMounts:
        - name: config-volume
          mountPath: /app/appsettings.json
          subPath: appsettings.json
      volumes:
      - name: config-volume
        configMap:
          name: exam-appsettings
      imagePullSecrets:
      - name: tcloud
---
apiVersion: v1
kind: Service
metadata:
  name: exam
  namespace: code-spirit
  labels:
    k8s-app: exam
    qcloud-app: exam
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 8080
  selector:
    k8s-app: exam
    qcloud-app: exam